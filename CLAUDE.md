# CLAUDE.md

This file provides guidance to Claude Code (and other AI assistants) when working with code in this repository.

> **Status:** This repository is currently **empty** — it has no source code, build
> configuration, or commit history yet. This file is a scaffold establishing baseline
> conventions. **Update it as soon as real code lands**, replacing the placeholder
> sections below with concrete details (build commands, architecture, etc.).

## Repository Overview

- **Name:** Thathedvika/Thathedvika
- **Purpose:** _TODO: Describe what this project does and who it is for._
- **Primary language / stack:** _TODO: Fill in once the tech stack is chosen._

## Getting Started

_TODO: Once the project is initialized, document how to set up a local environment._

```bash
# Example placeholders — replace with real commands:
# Install dependencies
# <package-manager> install

# Run the project
# <run-command>
```

## Common Commands

Document the canonical commands here so assistants don't have to guess. Replace the
placeholders below as tooling is added.

| Task        | Command            |
| ----------- | ------------------ |
| Install     | _TODO_             |
| Build       | _TODO_             |
| Run / start | _TODO_             |
| Test        | _TODO_             |
| Lint        | _TODO_             |
| Format      | _TODO_             |

## Architecture & Structure

_TODO: Describe the high-level structure once directories exist — the main modules,
how they interact, and where key logic lives. Focus on the "big picture" that isn't
obvious from reading a single file._

## Conventions

Until project-specific conventions are established, follow these defaults:

- **Match existing style.** When code exists, mirror the surrounding naming,
  formatting, and patterns rather than introducing new ones.
- **Keep changes focused.** Make the smallest change that accomplishes the task.
- **Test what you change.** Add or update tests alongside behavioral changes once a
  test framework is in place.
- **Don't commit secrets.** Keep credentials and tokens out of the repository; use
  environment variables or an ignored local config file.

## Git Workflow

- Development happens on feature branches; do not commit directly to the default
  branch unless explicitly instructed.
- Write clear, descriptive commit messages explaining the "why" of a change.
- Do not create pull requests unless explicitly asked.

## Notes for AI Assistants

- **This file is the source of truth for project conventions.** When you learn
  something durable about how this codebase works (a build quirk, a required
  environment variable, an architectural decision), record it here.
- Keep this document accurate. If you change a workflow or command, update the
  relevant section in the same change.
